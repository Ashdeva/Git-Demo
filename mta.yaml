_schema-version: "3.2"
ID: nsgitdemoapp
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: nsgitdemoapp-destination-content
  type: com.sap.application.content
  requires:
  - name: nsgitdemoapp-destination-service
    parameters:
      content-target: true
  - name: nsgitdemoapp-repo-host
    parameters:
      service-key:
        name: nsgitdemoapp-repo-host-key
  - name: nsgitdemoapp-uaa
    parameters:
      service-key:
        name: nsgitdemoapp-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: nsgitdemoapp_html_repo_host
          ServiceInstanceName: nsgitdemoapp-html5-srv
          ServiceKeyName: nsgitdemoapp-repo-host-key
          sap.cloud.service: nsgitdemoapp
        - Authentication: OAuth2UserTokenExchange
          Name: nsgitdemoapp_uaa
          ServiceInstanceName: nsgitdemoapp-xsuaa-srv
          ServiceKeyName: nsgitdemoapp-uaa-key
          sap.cloud.service: nsgitdemoapp
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: nsgitdemoapp-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: nsgitdemoapp-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - nsgitdemoapp.zip
      name: nsgitdemoapp
      target-path: resources/
- name: nsgitdemoapp
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: nsgitdemoapp-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: nsgitdemoapp-destination-service
    service-plan: lite
- name: nsgitdemoapp-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: nsgitdemoapp-xsuaa-srv
    service-plan: application
- name: nsgitdemoapp-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: nsgitdemoapp-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
